import {
  getLocalStorage,
  saveLocalStorage,
} from './localStorage';

import {
  sendCode,
  sendAccount,
} from './sendEmail';

import saveUserContext from './saveUsers';

import replaceNames from './replace';

export {
  getLocalStorage,
  saveLocalStorage,
  sendCode,
  sendAccount,
  saveUserContext,
  replaceNames,
};
